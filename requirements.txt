# ─── Core (Remove BG + Resize + server) ───
flask>=3.0.0
rembg[gpu]>=2.0.0
pillow>=10.0.0
pillow-heif>=0.16.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
python-dotenv>=1.0.0
gunicorn>=21.0.0

# ─── Upscale tab (Real-ESRGAN via ONNX) ───
# Tidak butuh dependensi tambahan: jalan di onnxruntime (sudah terbawa
# rembg[gpu]) dengan bobot models/*.onnx yang ikut repo.
# Torch/basicsr/realesrgan TIDAK dibutuhkan lagi (hanya untuk konversi
# one-off — lihat convert_to_onnx.py).
#
# Pin onnxruntime-gpu to CUDA-12-compatible versions.
# 1.25+ requires CUDA 13 (libcudart.so.13) which is not in our base image.
onnxruntime-gpu>=1.23.2,<1.25
