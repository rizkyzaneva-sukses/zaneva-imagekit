# ─── Core (Remove BG + Resize + server) ───
flask>=3.0.0
rembg[cpu]>=2.0.0
pillow>=10.0.0
pillow-heif>=0.16.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
python-dotenv>=1.0.0
gunicorn>=21.0.0

# ─── Optional: Upscale tab (Real-ESRGAN) ───
# Tidak dimasukkan ke core agar image Docker tetap ramping (torch berukuran besar).
# Install via setup.sh, atau manual:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install realesrgan basicsr
#   python download_models.py   # unduh bobot model ke folder models/
# Inkompatibilitas basicsr vs torchvision>=0.17 sudah ditangani otomatis oleh
# shim di modules/upscaler.py (_patch_torchvision_compat) — tidak perlu pin versi.
# Tanpa langkah ini, tab Upscale error saat dipakai; Remove BG & Resize tetap jalan.
